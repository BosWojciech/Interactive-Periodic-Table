import { GiAtom } from "react-icons/gi";

const Loader = () => {
    return (
        <div className="loader">
            <GiAtom />
            <p>Loading...</p>
        </div>
    );
};

export default Loader